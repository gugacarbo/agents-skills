# TypeScript requirements

## Core Dependencies

- `typescript`.

## Dev Dependencies

- `vitest`, `@biomejs/biome`, `knip`, and `husky`.

## CI/CD

- Type checking, tests, Biome, Knip, and build must pass on pull requests.

## Skills

- `project-init`, `commit-changes`, and `code-flow`.
