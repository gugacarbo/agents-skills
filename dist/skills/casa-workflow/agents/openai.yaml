interface:
  display_name: "CASA Workflow"
  short_description: "Guardrail do ciclo de mudanças CASA"
  default_prompt: "Use $casa-workflow para classificar tier, artefatos, gate e contexto durável desta mudança."
policy:
  allow_implicit_invocation: true
