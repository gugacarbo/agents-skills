# Provider watchdog template

- Provider identifier and host capability
- Default `.code-toolbox/watchdogs/<provider>-watchdogs.json` path
- Named profiles, roles, intervals, and independent prompt files
- Create/update/pause/disable and duplicate-prevention procedure
- Local metadata schema and terminal/human-block behavior
- Verification cases and provider-specific non-goals
