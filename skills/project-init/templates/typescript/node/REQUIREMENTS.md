# TypeScript and Node requirements

## Core Dependencies

- `typescript` and `@types/node` for the Node.js type boundary.

## Dev Dependencies

- `vitest`, `@biomejs/biome`, `tsx`, `knip`, and `husky`.

## CI/CD

- Run `tsc --noEmit`, Vitest, Biome, Knip, and the production build.
