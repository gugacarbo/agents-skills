# Exemplo de contrato de ambiente — copie para .env e preencha os valores reais.
# .env é ignorado pelo git; NUNCA comite segredos reais.
#
# Valide e centralize estas leituras antes do startup (ex.: @t3-oss/env-core + zod):
# exporte valores tipados a partir de um único módulo e consuma-os no app.
# Veja os planNotes da optional-tool "env" para o padrão recomendado.
