interface:
  display_name: "Code Flow"
  short_description: "Entrega por issue com gates e agentes"
  default_prompt: "Use $code-flow para coordenar esta entrega por issue com rigor proporcional ao risco."

policy:
  allow_implicit_invocation: false
