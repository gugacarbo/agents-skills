{
	"name": "@gugacarbo/skill-code-flow",
	"version": "0.0.0",
	"private": true,
	"description": "Development tooling for the code-flow skill.",
	"scripts": {
		"test": "bun test tests/tests.test.ts",
		"build": "bash -lc 'for file in scripts/*.sh; do bash -n \"$file\"; done'"
	}
}
